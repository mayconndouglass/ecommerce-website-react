import styled from 'styled-components'

export const Main = styled.main`
    .cards-container {
        gap: 3rem 1rem;
    }

    button {
        margin-top: 3rem;
        margin-bottom: 2.5rem;
    }
`
